using HseCore
using HseRender

const can_model: load_model("base/models/can.obj")

template Can {
	prop position = Position: {0,0,0}
	prop rotation = Rotation: {1,0,0,0}

	$position
	$rotation
	Velocity

	$can_model
}

Can can()
